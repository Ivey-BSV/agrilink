"use client";

export default function AdminDocumentsQueuePage() {
  return (
    <div className="content-card stack" style={{ gap: 10 }}>
      <p className="subtle" style={{ margin: 0 }}>
        Upload approvals are turned off in the dashboard for now. Workshop and repository files can still be uploaded
        from their resource pages.
      </p>
    </div>
  );
}
